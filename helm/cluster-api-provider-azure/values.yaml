name: cluster-api-azure-controller
image:
  registry: quay.io
  name: giantswarm/cluster-api-azure-controller
  tag: daily-202108171000

featuregates:
  machinepool: true
  aks: false

webhook:
  port: 9443

project:
  branch: "[[ .Branch ]]"
  commit: "[[ .SHA ]]"
